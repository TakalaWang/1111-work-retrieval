[project]
name = "work-retrieval-core"
version = "0.1.0"
description = "Stable interface between the job-search API and its future retrieval engine."
requires-python = "==3.12.*"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/work_retrieval_core"]
