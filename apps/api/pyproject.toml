[project]
name = "work-retrieval-api"
version = "0.1.0"
description = "HTTP contract for the 1111 job retrieval platform."
requires-python = "==3.12.*"
dependencies = [
    "fastapi>=0.116,<1",
    "work-retrieval-core",
    "work-retrieval-database",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/work_retrieval_api"]

[tool.uv.sources]
work-retrieval-core = { workspace = true }
work-retrieval-database = { workspace = true }
